name: smart_fridge
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0<3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  animated_text_kit: ^4.2.1
  firebase_core: ^1.7.0
  firebase_auth: ^3.1.3
  cloud_firestore: ^2.5.3
  modal_progress_hud: ^0.1.3
  provider: ^6.0.1
  intl: ^0.17.0
  tflite: ^1.1.2
  camera: ^0.5.8+7
  flutter_local_notifications: ^5.0.0+1
  firebase_storage: ^10.0.3
  image_picker: ^0.8.4+9
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
  - assets/images/
  - assets/model/ssd_mobilenet.tflite
  - assets/model/labels.txt
